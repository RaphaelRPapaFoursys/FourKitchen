spring:
  application:
    name: db-migrations
  datasource:
    url: jdbc:mysql://localhost:3306/db_restaurante?createDatabaseIfNotExist=true
    username: root
    password: aluno1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration