spring:
  application:
    name: ms-mesas
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 8
      minimum-idle: 1
      idle-timeout: 20000
      connection-timeout: 10000
      max-lifetime: 60000
server:
  port: ${SERVER_PORT:8082}

clients:
  ms-pedidos:
    url: ${MS_PEDIDOS_URL:http://localhost:8083}
