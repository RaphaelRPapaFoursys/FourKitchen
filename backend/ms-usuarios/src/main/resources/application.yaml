spring:
  application:
    name: ms-usuarios
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/db_restaurante?createDatabaseIfNotExist=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:aluno1234}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none

server:
  port: ${SERVER_PORT:8081}

jwt:
  secret: ${JWT_SECRET:chave-super-secreta-para-jwt-local-fourkitchen-123456789}
  expiration-ms: ${JWT_EXPIRATION_MS:3600000}
